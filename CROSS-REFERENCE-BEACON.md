# 🚨 Cross-Reference Beacon

This file was planted by a Claude Code **web/cloud session** to let a second Claude
session confirm whether both are operating in the same GitHub repository.

If your session can see this file, **you are in the same repo and branch as the
session that wrote it.** If you cannot, the two sessions are in different spaces.

## Unique beacon token

```
BEACON-COMPASSUS-CCSI-2026-07-22-HHT-a267
```

Search GitHub / your working tree for that token to confirm a match.

## Exact coordinates of the writing session

| Field | Value |
| --- | --- |
| Remote | `worker-max/Home-Health-Tools` |
| Branch | `claude/home-health-project-manager-j5ntcd` |
| Beacon commit is a child of | `8da773e` (the PM-agent commit) |
| `main` branch present? | **No** — this repo has no `main`; the only branch is the one above |
| Commit `59b2a46` present here? | **No** — not in this repo's history at all |
| Date written | 2026-07-22 |

## What else is in this repo (so you can eyeball a match)

- `agents/compassus-capacity-pm/AGENT.md` — Home Health Expert PM agent definition
- `agents/compassus-capacity-pm/initiative-playbook.md` — phased Discover→Design→Pilot→Scale→Sustain program
- `.claude/agents/compassus-capacity-pm.md` — runnable Claude Code subagent
- `README.md` — repo orientation

## If your session does NOT see this

Then you are looking at a different repository (e.g. one containing a `ccsi`
directory, or one whose `main` is at `59b2a46`). Compare your `git remote -v`
and `git ls-remote --heads origin` against the coordinates above to find where
the two spaces diverge.
